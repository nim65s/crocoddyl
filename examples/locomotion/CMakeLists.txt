SET(${PROJECT_NAME}_LOCOMOTION_EXAMPLE_LOGS
  #talos_warm_start
  )

FOREACH(EXAMPLE ${${PROJECT_NAME}_LOCOMOTION_EXAMPLE_LOGS})
  CHECK_LOG(${EXAMPLE})
ENDFOREACH(EXAMPLE ${${PROJECT_NAME}_LOCOMOTION_EXAMPLE_LOGS})
